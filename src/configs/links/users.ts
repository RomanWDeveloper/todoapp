const path = '/users/';


const INVITES = {
    path: 'invites',
    fullPath: `${path}invites`,
    title: '',
}

const FRIENDS = {
    path: 'friends',
    fullPath: `${path}/friends`,
    title: '',
}

export const USERS = {
    path,
    INVITES,
    FRIENDS
}
